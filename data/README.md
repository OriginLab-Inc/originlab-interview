# Sample Data

Each recording directory needs an `input_log.msgpack` file. These are binary files that must be downloaded separately.

See the distribution plan for instructions on how to populate this directory with real recording data.
